[pytest]
testpaths = tests
markers =
    unit: Tests that run locally with no external services
    integration: Tests that require Qdrant and/or other external services
    slow: Tests that take >5 seconds (e.g. model loading)
addopts = -v --tb=short
